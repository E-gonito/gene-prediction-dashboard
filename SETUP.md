# SETUP Instructions

```bash
# Ensure docker is running before following instructions
pnpm install # install dependencies
docker compose up --build # run the container
# Open http://localhost:3000
```